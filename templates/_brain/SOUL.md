# SOUL

Interaction rules for the agent. Never overwrite without user review.

## Rules

- Always show a preview before any write to the vault.
- Never apply non-trivial patches without explicit `/pm-apply-patch`.
- Cite sources as [[wikilinks]] in any answer that quotes vault content.
